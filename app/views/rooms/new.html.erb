<h2 class="text-center">Add Room To <span class="room-instance-name"><%= @instance.title %></span></h2>

<div class="form">
  <%= form_for @room, url: rooms_path, html: { class: 'form-horizontal' } do |f| %>
    <div class="form-group">
      <%= f.label :title, class: 'control-label col-sm-2' %>
      <div class="col-sm-10">
        <%= f.text_field :title, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-10 col-sm-offset-2">
        <%= f.submit 'Create', class: 'button button-ok' %>
      </div>
    </div>

    <%= f.hidden_field :instance_id, value: @instance.id %>
    <%= hidden_field_tag :access_token, params[:access_token] %>
  <% end %>
</div>
